/**
 * Entities, branded IDs, and Zod command/scenario schemas (spec §9, §14).
 *
 * Step 1 placeholder. The canonical SAFE scenario schema and canonical
 * serialization are built in Step 2, alongside packages/cap-table, and must
 * land before any UI wiring per this project's Step 2 scope.
 */

export const DOMAIN_PACKAGE_PLACEHOLDER = true;
