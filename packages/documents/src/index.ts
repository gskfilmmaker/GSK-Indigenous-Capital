/**
 * Template schemas and DOCX/PDF renderers (spec §16). See ./CLAUDE.md for
 * this package's engineering rules.
 *
 * Step 1 placeholder. This package stays empty of any real template until
 * a named Canadian counsel admits one (spec §11 mandatory review gate 1,
 * §26 item 5) — building the rendering pipeline (ADR 0005) does not
 * require a real template to exist.
 */

export const DOCUMENTS_PACKAGE_PLACEHOLDER = true;
