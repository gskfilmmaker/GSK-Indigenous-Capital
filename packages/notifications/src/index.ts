/**
 * Email/reminder logic (spec §18). Transactional templates (invitation,
 * snapshot shared, counsel review, board evidence, document ready, signing,
 * payment, filing deadline, delivery, share link, security/privacy alert).
 *
 * Step 1 placeholder. Deferred beyond this project block; built alongside
 * the governance/document/compliance modules (spec Phase 4-5).
 */

export const NOTIFICATIONS_PACKAGE_PLACEHOLDER = true;
