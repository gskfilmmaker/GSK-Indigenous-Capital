/**
 * Design system and accessibility primitives (spec §6, "Design system to
 * establish first" in this project's prompt).
 *
 * Step 1 placeholder. Tokens (spacing/type scale, semantic colour roles
 * for light/dark, radii, motion, elevation) and primitives (DataTable,
 * NumberField, OwnershipBar/DilutionBridge, StateBadge, WatermarkOverlay,
 * DisclaimerBanner, StepIndicator) are built in Step 3, with Storybook +
 * axe wired into CI before any component is considered done.
 */

export const UI_PACKAGE_PLACEHOLDER = true;
