/**
 * Pure, deterministic, versioned SAFE/cap-table calculation engine
 * (spec §7, §8). See ./CLAUDE.md for this package's engineering rules.
 *
 * Step 1 placeholder. The engine — arbitrary-precision arithmetic, cap
 * SAFE math, mixed caps, priced-round route comparison, option-pool
 * solver, MFN ordering, and golden tests 1–6/10–15 — is built in Step 2,
 * before any UI wiring.
 */

export const CAP_TABLE_ENGINE_PLACEHOLDER = true;
