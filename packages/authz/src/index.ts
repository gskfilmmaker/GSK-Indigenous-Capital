/**
 * Capabilities and authorization helpers (spec §4.2).
 *
 * Step 1 placeholder. The capability list (org.read, ledger.post,
 * document.final.generate, etc.) and the TypeScript-side capability checks
 * that mirror the Postgres `security definer` functions from
 * docs/adr/0003-supabase-rls-strategy.md are built starting in Step 4.
 */

export const AUTHZ_PACKAGE_PLACEHOLDER = true;
