# docs/

- `adr/` — Architecture Decision Records (see `adr/README.md`).
- Threat model, privacy inventory (spec §12), and operational runbooks
  are Phase 0/Step 4+ deliverables, not yet written. Do not assume a
  privacy or security posture beyond what an ADR or root `CLAUDE.md`
  states explicitly.
